(function () {

    angular.module('myDealTask').controller('HomeController', ['$scope', '$location', '$filter', 'dataService', 'modalService', 'appConfig',
    function ($scope, $location, $filter, dataService, modalService, appConfig) {

        /*
        function init() {
            //dataService.eventsService.initialize().then(function(p) {
                $scope.updateEvents();
            //});
        }   
        init();*/
    }
    ]);
    
}());